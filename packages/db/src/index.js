// Tauri SQL plugin wrapper — called from the SvelteKit frontend via Tauri invoke.
// The actual SQLite connection lives in Rust (src-tauri/); this module provides
// typed JS helpers that call through to Tauri commands.

export { } from './types.js';
